/*! Pre-jQuery Conf Zebra Striping Plugin - v0.1.0 - 2012-06-27
* Copyright (c) 2012 John Q. Public; Licensed MIT, GPL */

(function($) {

  // This jQuery method should zebra-stripe all selected tables, adding
  // the specified className to all odd table rows.
  $.fn.zebraStripe1 = function(className) {
    // Your code goes here.
  };

}(jQuery));

(function($) {

  // This jQuery method should zebra-stripe all selected tables and lists,
  // adding the specified className to all odd table rows (or listitems).
  $.fn.zebraStripe2 = function(className) {
    // Your code goes here.
  };

}(jQuery));

(function($) {

  // This jQuery method should zebra-stripe all selected tables and lists,
  // adding the specified className to all odd or even table rows (or
  // listitems). Instead of specifying the options as individual arguments,
  // they should be specified as properties of an options object.
  $.fn.zebraStripe3 = function(options) {
    // Your code goes here.
  };

  // This default options object should default to a className of 'zebra'
  // and 'odd' (vs. 'even') row striping.
  $.fn.zebraStripe3.options = {
    // Your code goes here.
  };

}(jQuery));
